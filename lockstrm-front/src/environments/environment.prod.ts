// ⚠️  ANTES DE DESPLEGAR: sustituir apiUrl por la URL real del backend de producción.
//     Ejemplo: 'https://api.lockstrm.com'
//     Dejarlo vacío hace que todas las llamadas HTTP fallen con un error de URL relativa.
export const environment = {
  production: true,
  apiUrl: '',
};
